# Netlify deploy downloads
deploy-*.zip

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# Local tooling (the preview server and its launch config are tracked; the build scripts need them)
.claude/*
!.claude/serve.ps1
!.claude/launch.json
